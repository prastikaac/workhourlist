Create a complete modern Work Hour Tracker web application using HTML, CSS, JavaScript, Firebase Firestore, and Firebase SDK v11.

Use Firebase as the database instead of Local Storage.

Firebase Configuration:

```javascript
import { initializeApp } from "firebase/app";

const firebaseConfig = {
  apiKey: "AIzaSyAnBX2gLAo9aZRC8suyRNICRjoOUK4gp-4",
  authDomain: "real-time-prasiddha.firebaseapp.com",
  databaseURL: "https://real-time-prasiddha-default-rtdb.asia-southeast1.firebasedatabase.app",
  projectId: "real-time-prasiddha",
  storageBucket: "real-time-prasiddha.firebasestorage.app",
  messagingSenderId: "713785938695",
  appId: "1:713785938695:web:a9aa397c252a9eb9e9eba2"
};
```

Requirements:

### 1. Work Entry Form

Create a clean dashboard-style form with:

* Location Dropdown:

  * Kotkansiipi
  * Rautkallionkatu
  * Kakoisvayla
  * Mayavatie

* Date Picker

  * Automatically set to tomorrow's date when page loads.

* Check-In Time

* Check-Out Time

* Notes Field

* Automatically calculate working hours after selecting check-in and check-out times.

* Save Entry Button

### 2. Firestore Database Structure

Collection Name:

```text
workHours
```

Document Structure:

```javascript
{
  date: "2026-06-08",
  location: "Mayavatie",
  checkIn: "08:00",
  checkOut: "10:00",
  totalHours: 2,
  note: "Cleaning work",
  createdAt: serverTimestamp()
}
```

### 3. Save to Firestore

When Save is clicked:

* Calculate total hours automatically.
* Save record to Firestore.
* Show success message.
* Clear form except date.
* Refresh table automatically.

### 4. Real-Time Firestore Updates

Use:

```javascript
onSnapshot()
```

Requirements:

* Listen for changes in Firestore.
* Automatically update the table when records are added, edited, or deleted.
* No page refresh required.

### 5. Work Hours Table

Create a professional responsive table.

Columns:

* Date
* Location
* Check In
* Check Out
* Hours
* Notes
* Actions

Actions:

* Edit
* Delete

### 6. Edit Feature

When Edit is clicked:

* Load record into form.
* Change Save button to Update Entry.
* Update Firestore document.
* Return button to Save mode after updating.

### 7. Delete Feature

When Delete is clicked:

* Show confirmation dialog.
* Delete Firestore document.
* Update table instantly through onSnapshot.

### 8. Dashboard Statistics

Create dashboard cards showing:

* Total Hours Worked
* Total Entries
* Hours at Kotkansiipi
* Hours at Rautkallionkatu
* Hours at Kakoisvayla
* Hours at Mayavatie

Statistics should update automatically from Firestore data.

### 9. Filtering

Add filters:

* Location Filter
* Month Filter
* Start Date
* End Date
* Search Notes

Filters should work instantly on the loaded records.

### 10. Export Features

Add:

* Export CSV
* Export Excel (.xlsx)

Export currently filtered data.

### 11. UI Design

Create a modern professional dashboard with:

* Responsive mobile-first layout
* Sidebar or top navigation
* Dashboard statistic cards
* Modern glassmorphism/card design
* Smooth animations
* Icons
* Loading spinner
* Empty state when no records exist

### 12. Dark Mode

Add:

* Dark Mode Toggle
* Save preference in localStorage

### 13. Firebase Setup

Use Firebase SDK v11 modules:

```javascript
import { initializeApp } from "firebase/app";

import {
  getFirestore,
  collection,
  addDoc,
  updateDoc,
  deleteDoc,
  doc,
  onSnapshot,
  query,
  orderBy,
  serverTimestamp
} from "firebase/firestore";
```

Initialize:

```javascript
const app = initializeApp(firebaseConfig);
const db = getFirestore(app);
```

### 14. Project Files

Generate complete production-ready code for:

* index.html
* style.css
* script.js

### 15. Additional Improvements

Add:

* Validation for required fields.
* Prevent checkout time earlier than check-in time.
* Toast notifications.
* Loading indicators.
* Real-time dashboard updates.
* Sort entries by newest date first.
* Clean reusable code structure.

The final result should look like a professional employee time-tracking dashboard and be fully functional with Firebase Firestore.
